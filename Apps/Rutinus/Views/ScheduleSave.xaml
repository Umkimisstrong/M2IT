<toolkit:Popup
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Class="Rutinus.ScheduleSave"
    CanBeDismissedByTappingOutsideOfPopup="True"
    
    WidthRequest="350"
    HeightRequest="600">

    <Border
        BackgroundColor="White"
        Stroke="LightGray"
        StrokeShape="RoundRectangle 10"
        Padding="20">

        <!-- 전체 레이아웃 Grid -->
        <Grid RowDefinitions="Auto,Auto,Auto,*,Auto" RowSpacing="15">

            <!-- 1. 상단 헤더 -->
            <Grid Row="0" ColumnDefinitions="*,Auto">
                <Label x:Name="lblHeader"
                       Text="루틴 일정 추가"
                       FontSize="18"
                       FontAttributes="Bold"
                       VerticalOptions="Center"/>
                <Button Text="✕"
                        BackgroundColor="Transparent"
                        FontSize="20"
                        TextColor="Gray"
                        HorizontalOptions="End"
                        x:Name="btnClose"
                        Clicked="btnClose_Clicked"/>
            </Grid>

            <!-- 2. 루틴 목록 -->
            <Border Grid.Row="1"
                    Stroke="LightGray"
                    StrokeShape="RoundRectangle 8"
                    Padding="10">
                <VerticalStackLayout>
                    <Label Text="루틴 목록" FontAttributes="Bold" Margin="0,0,0,5"/>
                    <CollectionView x:Name="RoutineList" SelectionMode="Multiple">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="Auto,*" Padding="5">
                                    <CheckBox IsChecked="{Binding IsSelected}" />
                                    <VerticalStackLayout Grid.Column="1" Spacing="2">
                                        <Label Text="{Binding RoutineName}" FontAttributes="Bold"/>
                                        <Label Text="{Binding RoutinePartName}" FontSize="12" TextColor="Gray"/>
                                    </VerticalStackLayout>
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

            <!-- 3. 상하 버튼 (▲▼) -->
            <VerticalStackLayout Grid.Row="2" HorizontalOptions="Center" Spacing="10">
                <Button Text="▲" WidthRequest="40" Clicked="OnAddRoutineClicked"/>
                <Button Text="▼" WidthRequest="40" Clicked="OnRemoveRoutineClicked"/>
            </VerticalStackLayout>

            <!-- 4. 추가된 루틴 목록 -->
            <Border Grid.Row="3"
                    Stroke="LightGray"
                    StrokeShape="RoundRectangle 8"
                    Padding="10">
                <VerticalStackLayout>
                    <Label Text="추가된 루틴" FontAttributes="Bold" Margin="0,0,0,5"/>
                    <CollectionView x:Name="SelectedRoutineList">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*,Auto" Padding="5" ColumnSpacing="10">
                                    <VerticalStackLayout>
                                        <Label Text="{Binding RoutineName}" FontAttributes="Bold"/>
                                        <Label Text="{Binding TrainingNm}" FontSize="12" TextColor="Gray"/>
                                    </VerticalStackLayout>

                                    <!-- 시간 선택 -->
                                    <Picker Grid.Column="1"
                                            ItemsSource="{Binding TimeSlots}"
                                            SelectedItem="{Binding ScheduleHms}"
                                            WidthRequest="120"/>
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>

            <!-- 5. 하단 버튼 -->
            <Grid Grid.Row="4" ColumnDefinitions="Auto,Auto" ColumnSpacing="10" HorizontalOptions="Center">
                <Button Text="초기화" Clicked="OnResetClicked"
                        BackgroundColor="#EEEEEE" TextColor="Black" WidthRequest="100"/>
                <Button Text="저장" Clicked="OnSaveClicked"
                        BackgroundColor="#007AFF" TextColor="White" WidthRequest="100"/>
            </Grid>

        </Grid>
    </Border>
</toolkit:Popup>
