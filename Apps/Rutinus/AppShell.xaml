<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Rutinus.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Rutinus"
    FlyoutBehavior="Disabled">

    <!-- 로그인 단계 -->
    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />

    <ShellContent
        Title="SignUp"
        ContentTemplate="{DataTemplate local:SignUpPage}"
        Route="SignUpPage" />

    <!-- 메인 이후 단계 -->
    <ShellItem Route="MainPage">
        <ShellSection>
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </ShellSection>
    </ShellItem>

    <!-- 하위 페이지 (MainPage 아래에서 이동할 대상들) -->
    <ShellContent
        Title="RoutineList"
        ContentTemplate="{DataTemplate local:RoutineList}"
        Route="RoutineList" />

    <!--<ShellContent
        Title="RoutineSave"
        ContentTemplate="{DataTemplate local:RoutineSave}"
        Route="RoutineSave" />-->

    <ShellContent
        Title="TrainingSaveList"
        ContentTemplate="{DataTemplate local:TrainingSaveList}"
        Route="TrainingSaveList" />
</Shell>